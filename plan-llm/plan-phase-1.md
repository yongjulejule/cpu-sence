# phase 1 dir 구조

```
phase-1-cpu-basics/
├── 1-integer-vs-float
│   ├── integer_ops.c         # 정수 연산(덧셈, 곱셈 등) 실습 예제
│   ├── float_ops.c           # 실수 연산(pow, sqrt 등) 실습 예제
├── 2-loop-branch-recursion
│   ├── iterative_sum.c    # iterative sum 
│   ├── recursion_sum.c    # recursion sum
├── 3-memory-operations
│   ├── memory_alloc.c        # malloc, memset, memcpy 등 메모리 연산 실습 예제
├── Makefile                  # 전체 빌드와 실행을 위한 자동화 스크립트
├── record.sh                 # 각 실습 실행 결과(시간, perf 결과)를 기록하는 스크립트
└── results.md                # 각 실습 결과를 테이블 형식으로 정리해둘 문서

```


